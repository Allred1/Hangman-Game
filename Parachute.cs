// Class Parachute

class Parachute {

    // display the word at top (with each letter that's guessed correctly)
    // display lines of parachute
    // display the guy and the ground
    
    WordBank wordBank = new WordBank();


    public string displayWord() {
        // length of the word
        // print " _ " for each letter

        // string word = wordBank.DrawRandomWord();
        int wordLength = word.Length;
        // for each letter in the word, print an underscore
        return word;


    } 

    // cut 1 line of parachute if guess is wrong
    // leave alone if guess is correct

    // if all lines are cut, replace the head with x (END the GAME)
    
}